A slim determinate progress bar — Curate's triage progress.

```jsx
<ProgressBar value={triaged} max={total} label="Triage progress" showValue />
```

Tones: brand / info / star. Keep it thin (6px). Pair with a caption for context.
