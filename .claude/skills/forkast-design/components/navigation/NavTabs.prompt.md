The app's top tab bar. Active tab = brand-tint pill; others muted. Scrolls
horizontally on narrow screens.

```jsx
<NavTabs active={tab} onChange={setTab}
  tabs={[{id:'browse',label:'Browse'},{id:'plan',label:'Plan'},{id:'shop',label:'Shop'}]} />
```
