A square ghost icon button for chrome actions (theme toggle, remove ✕, row menu ▾).

```jsx
<IconButton label="Remove from week" tone="danger">✕</IconButton>
<IconButton label="Dark mode">🌙</IconButton>
```

Tone shifts hover colour (neutral / danger / brand). Pass a glyph or an SVG icon
node as children.
