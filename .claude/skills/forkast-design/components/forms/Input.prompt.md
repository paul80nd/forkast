A text / search field with the brand focus ring.

```jsx
<Input type="search" placeholder="Search title or ingredient…" value={q} onChange={e=>setQ(e.target.value)} />
```

Full-width by default within its container. Forwards all native input props.
